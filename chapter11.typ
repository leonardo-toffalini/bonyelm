#import "@preview/thmbox:0.3.0": *
#import "@preview/fletcher:0.5.8" as fletcher: diagram, node, edge, shapes
#import "@preview/cetz:0.4.2": canvas, draw, tree
#show: thmbox-init()

#let P = $"P"$
#let NP = $"NP"$
#let Ppoly = $"P"\/"poly"$

#let definition = definition.with(
  variant: "Definíció"
)

#let theorem = theorem.with(
  variant: "Tétel"
)

#let remark = remark.with(
  variant: "Megjegyzés"
)

#let corollary = corollary.with(
  variant: "Következmény"
)

#let example = example.with(
  variant: "Példa"
)

#let proposition = proposition.with(
  variant: "Állítás"
)

= Kolmogorov bonyolultság

Kolmogorov bonyolultság def

Kolm bony jol definialt

Lemma: `print "x"`

Tetel: A Kolm bony csak konstans erejeig fugg a TG-tol (invarencia)

Tetel: A kolm bony nem rekurziv

Tetel: Kozelithetetlenseg

Tetel: Keves olyan szo van aminek a Kolm bonyolultsága kicsi

Kov: 99%-a a szavaknak Kolm bony $>= n - 8$

Informatikusan veletlen sorozat

Szamitogep nem tud informatikusan veletlen sorozatot generalni

Formalis axioma rendszer merete def

Tetel: Veges meretu axioma rendszerben legfeljebb veges sok $m$-re lehet bizonyitani, hogy $K(x) > m$.

#pagebreak()

